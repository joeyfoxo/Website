server {
    listen 80;
    server_name joeyfox.dev;

    # Allows drag-and-drop file transfers up to 50MB
    client_max_body_size 50M;

    # 1. Serve the React Frontend
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 2. Proxy requests to the Spring Boot Backend
    location /api/ {
        # CRITICAL: No trailing slash here, so it passes "/api/auth/..." directly to Spring
        proxy_pass http://backend:8080;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}